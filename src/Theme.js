import { createTheme } from "@mui/material";

// setup theme
const theme = createTheme({
  palette: {
    primary: { main: "#ffa500" },
    secondary: { main: "#5342fc" },
  },
});

export { theme };
